import 'package:flutter/material.dart';

class SafetyPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Safety Measures"),),
      body: Container(),
    );
  }
}
